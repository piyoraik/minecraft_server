---
- name: Minecraft 運用用 wrapper script を配置する
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ wrapper_scripts_dir }}/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - mc-start
    - mc-stop
    - mc-status
    - mc-command
