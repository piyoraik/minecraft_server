---
- name: wrapper script を配置
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ wrapper_scripts_dir }}/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - mc-start
    - mc-stop
    - mc-status
    - mc-command
