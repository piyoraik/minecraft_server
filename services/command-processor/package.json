{
  "name": "@minecraft/command-processor",
  "version": "0.1.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/command-processor/test/**/*.test.js",
    "lint": "npm run typecheck"
  },
  "dependencies": {
    "@minecraft/shared": "0.1.0",
    "@aws-sdk/client-ec2": "^3.883.0",
    "@aws-sdk/client-secrets-manager": "^3.883.0",
    "@aws-sdk/client-ssm": "^3.883.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
